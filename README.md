# latex_test

This is a LaTeX test project created with VS Code and LaTeX Workshop.

## Files

- `main.tex`: LaTeX source file
- `main.pdf`: compiled PDF output
- `.gitignore`: ignored LaTeX auxiliary files

## Build

Use XeLaTeX:

```bash
latexmk -xelatex main.tex
